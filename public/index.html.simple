<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photography Management System - Professional Platform</title>
    <meta name="description" content="Complete photography business management platform with session scheduling, client galleries, and professional tools.">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-gold: #d4af37;
            --primary-dark: #1a1a1a;
            --text-light: #ffffff;
            --text-muted: #cccccc;
            --accent-sage: #87a96b;
            --bg-overlay: rgba(0, 0, 0, 0.7);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            color: var(--text-light);
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero-section {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: 
                linear-gradient(var(--bg-overlay), var(--bg-overlay)),
                url('https://images.unsplash.com/photo-1492691527719-9d1e07e534b4?w=1920&q=80') center/cover;
            text-align: center;
            padding: 0 20px;
        }

        .hero-content {
            max-width: 800px;
            animation: fadeInUp 1s ease-out;
        }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 8vw, 4.5rem);
            font-weight: 900;
            color: var(--primary-gold);
            margin-bottom: 1.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .hero-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            color: var(--text-muted);
            line-height: 1.4;
        }

        .hero-description {
            font-size: 1.1rem;
            margin-bottom: 3rem;
            line-height: 1.6;
            color: var(--text-light);
            opacity: 0.9;
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 30px;
            font-size: 1.1rem;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-gold) 0%, #b8941f 100%);
            color: var(--primary-dark);
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(212, 175, 55, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: var(--text-light);
            border: 2px solid var(--primary-gold);
        }

        .btn-secondary:hover {
            background: var(--primary-gold);
            color: var(--primary-dark);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Features preview section */
        .features-preview {
            padding: 80px 20px;
            background: linear-gradient(135deg, #2d2d2d 0%, #1a1a1a 100%);
            text-align: center;
        }

        .features-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            color: var(--primary-gold);
            margin-bottom: 2rem;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .feature-item {
            background: rgba(255,255,255,0.05);
            padding: 1.5rem;
            border-radius: 8px;
            border: 1px solid rgba(212, 175, 55, 0.2);
        }

        .feature-item h3 {
            color: var(--primary-gold);
            margin-bottom: 1rem;
            font-size: 1.2rem;
        }

        .feature-item p {
            color: var(--text-muted);
            line-height: 1.5;
        }

        /* Mobile responsive */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            
            .hero-subtitle {
                font-size: 1.2rem;
            }
            
            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 100%;
                max-width: 300px;
            }
        }
    </style>
</head>
<body>
    <div class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">Photography Management System</h1>
            <p class="hero-subtitle">Professional Business Platform for Photographers</p>
            <p class="hero-description">
                Streamline your photography business with our comprehensive management platform. 
                Handle client bookings, manage galleries, process invoices, and grow your business 
                with powerful tools designed for professional photographers.
            </p>
            <div class="cta-buttons">
                <a id="launchAppBtn" href="secure-login.html" class="btn btn-primary">Launch Application</a>
                <a href="#features" class="btn btn-secondary">Learn More</a>
            </div>
        </div>
    </div>

    <div id="features" class="features-preview">
        <h2 class="features-title">Everything You Need to Manage Your Photography Business</h2>
        <div class="features-grid">
            <div class="feature-item">
                <h3>📅 Session Management</h3>
                <p>Schedule and track photography sessions with comprehensive client management tools.</p>
            </div>
            <div class="feature-item">
                <h3>🖼️ Gallery Management</h3>
                <p>Organize and share client galleries with secure access and download capabilities.</p>
            </div>
            <div class="feature-item">
                <h3>💰 Invoicing & Payments</h3>
                <p>Generate professional invoices and manage payments with integrated billing systems.</p>
            </div>
            <div class="feature-item">
                <h3>📱 Mobile Ready</h3>
                <p>Access your business on-the-go with our responsive design and mobile apps.</p>
            </div>
            <div class="feature-item">
                <h3>☁️ Cloud Storage</h3>
                <p>Secure cloud storage with automatic backups and unlimited file management.</p>
            </div>
            <div class="feature-item">
                <h3>🤖 AI-Powered Tools</h3>
                <p>Generate blog content and optimize your workflow with intelligent automation.</p>
            </div>
        </div>
    </div>

    <script>
        // Android external browser authentication flow
        document.getElementById('launchAppBtn')?.addEventListener('click', (e) => {
            // More comprehensive Capacitor and Android detection
            const isCapacitor = !!(window.Capacitor || window.capacitor);
            const isAndroid = /Android/i.test(navigator.userAgent);
            const isWebView = !!(window.chrome && window.chrome.webstore === undefined);
            const hasNativeMethod = !!(window.open && window.open.toString().includes('[native code]'));
            
            console.log('🚀 LAUNCH APP CLICKED - DEBUG INFO:', { 
                isCapacitor, 
                isAndroid, 
                isWebView,
                hasNativeMethod,
                userAgent: navigator.userAgent,
                windowCapacitor: !!window.Capacitor,
                windowcapacitor: !!window.capacitor,
                href: window.location.href
            });
            
            // For now, let's force external browser for ALL Android devices
            if (isAndroid) {
                // For Android: Use external browser authentication
                e.preventDefault(); // Prevent default link navigation
                
                console.log('📱 ANDROID DETECTED: Opening external browser for authentication...');
                
                // Get current domain for the redirect URL
                const currentDomain = window.location.origin;
                
                // Create authentication URL with Android-specific parameters
                const authUrl = `${currentDomain}/secure-login.html?android=true&redirect=photomanager://auth/success`;
                
                console.log('🌐 Attempting to open browser with URL:', authUrl);
                
                // Try multiple methods to open external browser
                try {
                    // Method 1: Standard window.open with _system
                    const opened = window.open(authUrl, '_system');
                    console.log('Method 1 (window.open _system) result:', opened);
                    
                    // Method 2: If Capacitor is available, try Browser plugin
                    if (window.Capacitor && window.Capacitor.Plugins) {
                        setTimeout(async () => {
                            try {
                                const { Browser } = window.Capacitor.Plugins;
                                if (Browser) {
                                    await Browser.open({ url: authUrl });
                                    console.log('Method 2 (Capacitor Browser) executed successfully');
                                } else {
                                    console.log('Browser plugin not available');
                                }
                            } catch (browserError) {
                                console.error('Browser plugin error:', browserError);
                            }
                        }, 100);
                    }
                    
                    // Method 3: Direct location change as fallback
                    setTimeout(() => {
                        console.log('Method 3 (location change) executing...');
                        window.location.href = authUrl;
                    }, 200);
                    
                } catch (error) {
                    console.error('🚨 Error opening external browser:', error);
                    // Fallback to normal navigation
                    window.location.href = authUrl;
                }
                
            } else {
                // For web/desktop: Use normal navigation
                console.log('💻 WEB: Using normal navigation to secure-login.html');
                // Allow default <a> navigation to proceed
            }
        });

        // Android authentication return handler
        function handleAndroidAuthReturn() {
            console.log('🔄 ANDROID: Handling authentication return...');
            
            // Check if user has authenticated session
            fetch('/api/debug/session', {
                credentials: 'include',
                headers: {
                    'X-Android-App': 'true'
                }
            })
            .then(response => response.json())
            .then(data => {
                console.log('📊 SESSION CHECK:', data);
                
                if (data.authenticated && data.userId) {
                    console.log('✅ ANDROID: User authenticated, redirecting to app...');
                    // User is authenticated, navigate to the secure app
                    window.location.href = '/secure-app.html';
                } else {
                    console.log('❌ ANDROID: No authentication found');
                }
            })
            .catch(error => {
                console.error('🚨 SESSION CHECK ERROR:', error);
            });
        }

        // Listen for app events (when returning from browser)
        document.addEventListener('DOMContentLoaded', () => {
            // Check if this is Capacitor/Android environment
            if (window.Capacitor) {
                console.log('📱 CAPACITOR: Adding Android auth listeners...');
                
                // Listen for app state changes (when returning from browser)
                document.addEventListener('resume', () => {
                    console.log('📱 APP RESUMED: Checking for authentication...');
                    setTimeout(handleAndroidAuthReturn, 500); // Small delay to ensure session is set
                });
                
                // Listen for visibility changes (when app becomes visible again)
                document.addEventListener('visibilitychange', () => {
                    if (!document.hidden) {
                        console.log('👀 APP VISIBLE: Checking for authentication...');
                        setTimeout(handleAndroidAuthReturn, 500);
                    }
                });
                
                // Also check immediately if app was opened via deep link
                handleAndroidAuthReturn();
            }
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            });
        });
    </script>
</body>
</html>
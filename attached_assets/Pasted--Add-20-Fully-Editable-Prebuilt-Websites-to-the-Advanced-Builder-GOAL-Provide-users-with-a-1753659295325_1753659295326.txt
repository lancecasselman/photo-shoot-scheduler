// 🚀 Add 20 Fully Editable Prebuilt Websites to the Advanced Builder

// GOAL: Provide users with a professional library of 20 complete website templates
// Each includes: Home, About, Gallery, Contact pages – fully editable via the builder

// ✅ FILE STRUCTURE:
// presets/websites/
// ├── timelessWedding.js
// ├── boldMinimalist.js
// ├── beachVibes.js
// ├── moodyEditorial.js
// ├── lightAndAiry.js
// ├── etc (20 total)

// Each file exports:
// {
//   meta: { name: "Timeless Wedding", font: "Georgia", brandColor: "#d1bfa3", animations: true },
//   pages: {
//     home: [...block config...],
//     about: [...],
//     gallery: [...],
//     contact: [...]
//   }
// }

// 🔧 ADD TO SiteBuilder.jsx:

// Load all preset sites:
import presetSites from "./presets/websites"; // Combine all imports into an array

const [siteBlocks, setSiteBlocks] = useState(defaultEmptySite); // { home: [], about: [], ... }

const applyPresetSite = (preset) => {
  setSiteBlocks(preset.pages);
  setActiveTheme(preset.meta);
};

// Add dropdown or modal with previews:
// "Choose a Website Template" → renders 20 presets with name, font preview, and thumbnail (optional)

// When clicked → call applyPresetSite(preset)

// 🔧 UI Suggestion (in JSX):
<select onChange={(e) => {
  const selected = presetSites.find(site => site.meta.name === e.target.value);
  applyPresetSite(selected);
}}>
  <option value="">Choose a Design</option>
  {presetSites.map(site => (
    <option key={site.meta.name}>{site.meta.name}</option>
  ))}
</select>

// 🔧 Optional: Add thumbnail preview per preset
// Use a thumbnail field in meta (e.g. meta.thumbnail = "/images/preview_boldMinimalist.jpg")

// ✅ Save & Publish flow stays the same – just applies to full multi-page config

// 🔥 This gives users:
// - 20 starting points that look professional right away
// - Editable everything: text, fonts, photos, layout
// - Instant "WOW" factor out of the box
// - Way faster onboarding with pro-level design from Day 1

// 🔧 Later upgrades:
// - Tag presets: "Weddings", "Seniors", "Family", etc
// - "Favorite" presets per user
// - Filter by layout style (dark/light/minimalist/boho)
// - Let users clone & remix templates into custom saves
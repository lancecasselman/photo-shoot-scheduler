// TASK: Enhance the Visual Website Builder with full creative control features

/* OBJECTIVES:
1. Enable per-page background customization
2. Allow font customization per text group (headings, body, buttons)
3. Add working hamburger menu for live preview (mobile navigation)
4. Fix theme switching to properly update the live preview with selected styles
5. Ensure all updates reflect instantly in Live Preview
*/

// IMPLEMENTATION STEPS:

// === [1] BACKGROUND EDITING ===
- Add background color picker to sidebar under each Page tab
- On change, update the corresponding section’s style:
  e.g., `document.querySelector('.hero').style.backgroundColor = selectedColor`
- Persist background color in the Firebase layout JSON under `content.background`

// === [2] FONT EDITING ===
- In each editable block, define text groups (e.g. `.hero-title`, `.body-text`, `.button`)
- Add font selectors (dropdown) for: Headings, Body, Buttons
- Use Google Fonts API and inject via `<link href="">` or `<style>`
- Apply via JavaScript: `element.style.fontFamily = selectedFont`
- Store selections per block or globally in Firebase

// === [3] HAMBURGER MENU (Mobile Nav) ===
- Add hamburger toggle to top right in Live Preview
- Create a collapsible mobile nav menu with links to Home, About, Gallery, Store, Contact
- Toggle menu visibility with JavaScript
- Add smooth slide animation for polish

// === [4] FIX THEME SWITCHING ===
- Ensure each theme card loads the correct styles/templates from `/themes/{themeName}`
- On click:
  - Clear current layout
  - Load associated HTML + CSS
  - Apply layout JSON from theme preset
- Verify styles are applied to Live Preview DOM and not just the builder interface

// === [5] LIVE PREVIEW INSTANT UPDATE ===
- All edits (text, image, background, fonts) must:
  - Update DOM in real time
  - Persist to Firebase
  - Auto-reload from Firebase if refreshed
  - Use MutationObserver if needed to track live changes

// === [EXTRA POLISH] ===
- Ensure all page blocks are visually clean and styled to match selected theme
- Confirm responsive layout across Desktop, Tablet, Mobile modes
- Add editable page title per page (shown in tab + header)

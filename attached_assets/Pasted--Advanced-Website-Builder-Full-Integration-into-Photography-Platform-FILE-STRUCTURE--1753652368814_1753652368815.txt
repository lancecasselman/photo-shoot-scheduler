// Advanced Website Builder – Full Integration into Photography Platform

// 🔧 FILE STRUCTURE:
// /public/advanced-builder.html        ← Mount point for builder
// /components/SiteBuilder.jsx          ← Main builder container
// /components/BlockLibrary.jsx         ← Sidebar of addable blocks
// /components/LivePreview.jsx          ← Live real-time rendering
// /components/BlockEditor.jsx          ← (Optional) editing panel per block
// /components/firebase.js              ← Save/publish logic (Firebase-ready)

// ✅ FEATURES TO INCLUDE:

// 1. Sidebar: Add blocks (Heading, Paragraph, Image, Button)
// 2. Drag-to-add block flow (append to builder state)
// 3. Realtime live preview of layout (basic theme applied)
// 4. State saved as array of blocks with:
//    { id, type, content, styles: { fontSize, color, alignment } }
// 5. Save Button → saves siteConfig JSON to Firestore under current user
// 6. Publish Button → triggers Firebase Function to render and publish static HTML to /site/:username

// ⚙️ COMPONENT DETAILS:

// SiteBuilder.jsx
// - Uses useState for `blocks` array
// - Renders <BlockLibrary />, <LivePreview />, <Save/Publish buttons>
// - Handles adding new blocks, managing block IDs

// BlockLibrary.jsx
// - Left-hand sidebar
// - Buttons: "Add Heading", "Add Paragraph", "Add Image", "Add Button"
// - Each button calls addBlock(type) from props

// LivePreview.jsx
// - Renders blocks based on type
// - Supports inline styles from block.styles
// - Placeholder image for "image" type
// - Reorder logic optional for now

// (Optional) BlockEditor.jsx
// - When a block is selected, shows editing tools
// - Lets user change content, font size, alignment, color
// - Updates block state in SiteBuilder

// /public/advanced-builder.html
// - Loads React bundle
// - Renders <div id="root" />
// - Uses <script type="module" src="/src/index.jsx"></script>

// index.jsx
// - Mounts React app to #root
// - Imports SiteBuilder and renders it

// 🌐 Firebase Firestore Structure:
// users/{userId}/siteConfig → stores blocks, styles, username, brand color
// Firebase Function: `generateStaticSite(config, username)`
// - Renders HTML from config
// - Deploys to /site/:username

// 🧪 Dev Mode
// If dev flag is true (or user not logged in), use demo data:
// [
//   { id: 1, type: "heading", content: "Welcome to My Studio", styles: { fontSize: "36px", color: "#fff" } },
//   { id: 2, type: "paragraph", content: "This is a sample site.", styles: {} }
// ]

// 📌 GOAL:
// Build a Pixieset-style drag-and-drop editor for photographer clients
// Seamlessly integrated into the existing dashboard platform
// Fully customizable layouts, branding, and instant static site publishing
